# 08 — Leftover pins must not appear on other Frames

> *This was generated by AI during triage.*

**What to build:** A Geometric Prompt pin the labeler placed on Frame N shows only on Frame N. Today pins reported appearing on subsequent (propagated) Frames. Spec story 36: "leftover points not to copy onto Propagated Frames, so that pins stay on the Frame I clicked"; CONTEXT.md Geometric Memory is "for one Track on one Frame".

**Category:** bug

**Status:** resolved

- [x] Pin placed on Frame N does not render after scrubbing to any other Frame
- [x] Propagated Frames show no leftover pins (story 36)
- [x] Deleting a pin on Frame N leaves pins on other Frames untouched
- [x] Scrubbing back to Frame N still shows that Frame's own pins

## Comments

> *This was generated by AI during triage.*

**Triage notes (2026-09-08):** Reported by operator during real-GPU desk testing (post `fecb36d`). Verified facts:

- Backend is already frame-scoped: `endo_label/mask/session.py:156` stores `geometric_memory: dict[int, list]` per Track, and the per-frame state getter returns `self.geometric_memory.get(frame_index, [])` (`session.py:168`). So the leak is in the desk layer, not the data model.
- Suspects: `web/src/ClipDesk.tsx` leftover state is fetched once (session open / Predict) and not re-scoped on scrub, or `web/src/MaskOverlay.tsx` renders the `leftover` prop regardless of current `frameIndex`. Trace where the `leftover` prop value comes from and when it is refreshed on frame change.
- Verify the fix with a compose pytest if any backend call is touched. No Playwright in this ticket — e2e closeout for 08–10 lives in ticket 10; real-GPU desk eyeball stays operator-side.
- If the fix turns out to need a Geometric Memory data-model change (not just fetch/render scoping), stop and escalate to `/diagnosing-bugs` — do not reshape the domain model inside this ticket.

**Implemented (2026-09-08, commit `c5d7c76`):** Desk-layer fix only, as the triage notes suspected. Backend untouched. `web/src/ClipDesk.tsx` now tags its Session snapshot with the Frame it was fetched for (`sessionSnapshot: { frame, tracks } | null`), renders leftover pins only while `snapshot.frame === frameIndex`, re-fetches the snapshot on every Frame change while the Session is open (previously gated on pending marks — the leak), drops out-of-order fetch responses via `sessionFetchSeq`, and bumps the seq on Undo's in-hand snapshot write. Validation: `tsc` 0, `oxlint` 0 errors (1 pre-existing warning), vitest 54/54. No Playwright (ticket 10 owns e2e).
